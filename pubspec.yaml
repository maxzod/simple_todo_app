name: simple_todo_app
description: simple flutter todo app with bloc $ sqlite

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.0.0-nullsafety.5
  path: ^1.8.0
  path_provider: ^2.0.1
  queen_validators: ^0.2.1
  sqflite_common_ffi: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0

flutter:
  uses-material-design: true
